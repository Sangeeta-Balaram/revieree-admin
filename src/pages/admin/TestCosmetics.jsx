import { useState, useEffect, useRef } from 'react';
import { motion } from 'framer-motion';
import { Plus, Edit, Trash2, Palette, Search, Download, Upload, ArrowUpDown, ArrowUp, ArrowDown, X, FileVideo, Image as ImageIcon } from 'lucide-react';

const TestCosmetics = () => {
  return (
    <div>
      <h1>Test Cosmetics Page</h1>
      <button>Test Button</button>
    </div>
  );
};

export default TestCosmetics;